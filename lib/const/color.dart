import 'package:flutter/material.dart';

const backgroundColor = Color(0xFF1A202C);
const itemColor = Color(0xFF1F212D);
const bgSecondColor = Color(0xFF2D3548);
const primary = Color(0xFF33D4A0);
const textColor = Color(0xFF9BA2AB);
const gradientButton = LinearGradient(colors: [
  primary,
  primary,
]);
final gradientButtonOff = LinearGradient(colors: [
  Colors.white.withOpacity(0.06),
  Colors.white.withOpacity(0.06),
]);
const gradientTransparent = LinearGradient(colors: [
  Colors.transparent,
  Colors.transparent,
]);
// const gradientButtonGreen = LinearGradient(colors: [
//   Color(0xFF14C966),
//   Color(0xFF0ED82F),
// ]);
// final gradientButtonGrey = LinearGradient(colors: [
//   Colors.white.withOpacity(0.15),
//   Colors.white.withOpacity(0.15),
// ]);
// const gradientDisActiveTermsCategory =
//     LinearGradient(colors: [itemColor, itemColor]);
// const secondBackgroundColor = Color(0xFF282C3A);

const loadingColor = Color(0xFFFFFFFF);
